# 这里只是示例，你实际调用训练好的模型即可
def predict_sentiment(text):
    # 假设调用 LSTM/TextCNN 模型
    # 返回 label, score, 示例回复
    # 例如：
    label = "积极"
    score = 0.87
    reply = "太棒了！😊"
    return label, score, reply
